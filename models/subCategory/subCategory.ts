import { Category } from '../category/category';

export class subCategory{
  id: string;
  subCatName: string;
  isActive: boolean;
  categoryId: string;
  subColor: string;
  category: Category;
}

